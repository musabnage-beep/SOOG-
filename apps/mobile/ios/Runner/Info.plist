<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<!-- The UI is Arabic-only; without this the App Store listing claims the
	     app supports English only. -->
	<key>CFBundleLocalizations</key>
	<array>
		<string>ar</string>
	</array>
	<key>CFBundleDisplayName</key>
	<string>الضيافة</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>aldiafa</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>

	<!-- App uses only standard/exempt encryption (HTTPS) — skips TestFlight compliance prompt -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>

	<!-- Location permission (required for the address picker map). Only the
	     "while using" level is declared: the app never tracks in the
	     background, and declaring "always" invites an App Review question. -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>نحتاج إلى موقعك لتحديد عنوان التوصيل</string>

	<!-- No UIBackgroundModes. The bundle carries no GoogleService-Info.plist and
	     no aps-environment entitlement, so a push can never arrive, and nothing
	     refreshes in the background either. Claiming the modes anyway only
	     invites an App Review question. Restore "remote-notification" together
	     with the Firebase config file and the Push Notifications capability. -->

	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneClassName</key>
					<string>UIWindowScene</string>
					<key>UISceneConfigurationName</key>
					<string>flutter</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
					<key>UISceneStoryboardFile</key>
					<string>Main</string>
				</dict>
			</array>
		</dict>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<!-- Portrait-locked phone app: opt out of iPad multitasking so restricted
	     orientations are allowed (fixes App Store upload error 90474). -->
	<key>UIRequiresFullScreen</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<!-- Portrait-only for phone -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
	</array>
</dict>
</plist>
